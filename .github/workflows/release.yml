name: Release and Package

on:
  push:
    branches:
      - 'main'  # 仅当推送到 main 分支时触发

jobs:
  build:
    runs-on: windows-latest  # 使用 Windows 环境

    steps:
      # 检出代码
      - name: Checkout code
        uses: actions/checkout@v3

      # 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      # 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 使用 pyinstaller 打包 main.py
      - name: Package with PyInstaller
        run: |
          # 使用 PyInstaller 打包，生成项目名称.exe 格式的文件
          python -m PyInstaller -F --windowed main.py
          # 获取仓库名称（repo name）并重命名生成的文件
          $repoName = $env:GITHUB_REPOSITORY.Split('/')[1]  # 获取仓库名称
          $outputPath = "./dist/$repoName.exe"
          # 确保目标目录存在
          if (-not (Test-Path "./dist")) {
            New-Item -ItemType Directory -Path "./dist"
          }
          # 重命名生成的文件
          Rename-Item -Path "./dist/main.exe" -NewName $outputPath

      # 创建 Release 并上传打包文件
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.MY_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      # 上传打包文件到 Release
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.MY_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/$repoName.exe  # 生成的打包文件路径
          asset_name: $repoName.exe  # 固定的文件名
          asset_content_type: application/octet-stream
